<source>
  @type  forward
  @id    input1
  @label @mainstream
  port  24224
</source>

<label @mainstream>

  <match *.**>
    @type kafka_buffered
    zookeeper           zk:2181
    zookeeper_path      /kafka/brokers/ids

    output_data_type json
    default_topic log_log
    compression_codec gzip
    required_acks 1
  </match>

</label>
